#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int print_char(char a);
int print_str(char *str);
int _printf(const char *format, ...);

#endif /* header file */
